apiVersion: v1
kind: Service
metadata:
  name: grading-service
  labels:
    app: grading-service
spec:
  selector:
    app: grading-service
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP
